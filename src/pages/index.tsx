import Head from "next/head";
import { SignIn, SignInButton, SignOutButton, UserButton, useUser } from "@clerk/nextjs";
import Link from "next/link";
import { api } from "~/utils/api";
import Image from "next/image";
import { ClerkProvider, SignUp } from "@clerk/nextjs";
import { Component } from "react";
import { log } from "console";
import { Header } from "~/components/Header";

export default function Home() {
  const hello = api.example.hello.useQuery({ text: "from tRPC" });

  const user = useUser();
  console.log(user);
  console.log(hello.data)

  return (
    <>
      <Head>
        <title>Create T3 App</title>
        <meta name="description" content="Generated by create-t3-app" />
        <link rel="icon" href="/favicon.ico" />
      </Head>
      <main className="flex container min-h-screen flex-col bg-gradient-to-b from-[#2e026d] to-[#15162c]">
        <Header/>
        <h1 className="text-teal-100">Fantasy Judo - Make the sport personal</h1>
      </main>
    </>
  );
}
