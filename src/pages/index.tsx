import Head from "next/head";
import { SignIn, SignInButton, SignOutButton, UserButton, useUser } from "@clerk/nextjs";
import Link from "next/link";
import { api } from "~/utils/api";
import Image from "next/image";
import { ClerkProvider, SignUp } from "@clerk/nextjs";
import { Component } from "react";
import { log } from "console";
import { Header } from "~/components/Header";
import { Welcome } from "~/components/Welcome";

export default function Home() {
  const hello = api.example.hello.useQuery({ text: "from tRPC" });

  const user = useUser();
  const { isLoaded: userLoaded, isSignedIn} = useUser();
  
  
  console.log(user);
  console.log(hello.data)

  return (
    <>
      <Head>
        <title>Create T3 App</title>
        <meta name="description" content="Generated by create-t3-app" />
        <link rel="icon" href="/favicon.ico" />
      </Head>
      <main className="flex min-h-screen flex-col bg-[#2e1065]">
        <Header/>
        <Welcome/>

        <h1 className="text-teal-500 dark:text-white">Fantasy Judo - Make the sport personal</h1>
      </main>
    </>
  ); 
}
